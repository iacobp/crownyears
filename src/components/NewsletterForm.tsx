export default function NewsletterForm() {
  return (
    <div className="max-w-md border-l-2 border-gold pl-5" role="status">
      <p className="font-serif text-lg text-deep">The first edition is being prepared.</p>
      <p className="mt-2 text-sm leading-relaxed text-muted">
        Email sign-up opens when the newsletter pipeline is connected. No
        address is being collected yet.
      </p>
    </div>
  );
}
